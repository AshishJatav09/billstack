const express=require("express");const {summary}=require("../controllers/gst.controller");const auth=require("../middlewares/auth.middleware");const tenant=require("../middlewares/tenant.middleware");const {requireActiveSubscription}=require("../middlewares/subscription.middleware");const r=express.Router();r.use(auth,tenant,requireActiveSubscription());r.get("/summary",summary);module.exports=r;
